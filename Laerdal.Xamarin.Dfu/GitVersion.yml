mode: MainLine
branches:
  feature:
    tag: alpha
    increment: Patch
  develop:
    tag: beta
    increment: Patch
    regex: ^dev(elop)?(ment)?$
  master:
    tag: ''
    regex: (^master$|^origin\/master$|^main$|^origin\/main$)
    increment: Minor
  pull-request:
    tag: pr